{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "outputs": ["dist/**"],
      "inputs": ["src/**", "flight.ts", "vite.config.ts", "tsconfig.json"]
    }
  }
}

{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "pipeline": {
    "build": {
      "cache": false,
      "inputs": ["storybook-static/**"],
      "outputs": ["storybook-static/**"]
    }
  }
}

{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "cache": false,
      "inputs": ["storybook-static/**"],
      "outputs": ["storybook-static/**"]
    }
  }
}

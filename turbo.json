{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"]
    },
    "release": {},
    "lint": {},
    "test": {},
    "format": {},
    "ci": {
      "dependsOn": ["build", "release", "lint", "test", "format"]
    }
  }
}
